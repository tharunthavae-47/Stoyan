"use client"
import Link from "next/link"
import { useState } from "react"

export default function LoginPage(){const [message,setMessage]=useState("");function submit(e:React.FormEvent){e.preventDefault();setMessage("Supabase Auth wird im nächsten Schritt verbunden.")}return <main className="flex min-h-screen items-center justify-center px-6"><div className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-sm"><Link href="/" className="text-sm font-semibold text-blue-600">← Stoyan</Link><h1 className="mt-6 text-3xl font-black">Willkommen zurück</h1><p className="mt-2 text-slate-500">Melde dich bei deinem Konto an.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required type="email" placeholder="E-Mail-Adresse" className="w-full rounded-xl border border-slate-300 px-4 py-3"/><input required type="password" placeholder="Passwort" className="w-full rounded-xl border border-slate-300 px-4 py-3"/><button className="w-full rounded-xl bg-slate-900 py-3.5 font-bold text-white">Anmelden</button></form>{message&&<p className="mt-4 text-sm text-blue-600">{message}</p>}<p className="mt-6 text-center text-sm text-slate-500">Noch kein Konto? <Link href="/registrieren" className="font-bold text-blue-600">Registrieren</Link></p></div></main>}
