import Link from "next/link"
import { createClient } from "@/lib/supabase/server"

export default async function EmployeeDashboard() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return <main className="p-10"><Link href="/login" className="text-blue-600">Zum Login</Link></main>

  const { data: profile } = await supabase.from("profiles").select("first_name,last_name,city,avatar_url").eq("id", user.id).maybeSingle()
  const { data: employee } = await supabase.from("employee_profiles").select("profession,headline,years_experience,profile_visible,desired_employment_percent,preferred_radius_km").eq("id", user.id).maybeSingle()

  return <main className="min-h-screen bg-[#f7f8fa] text-slate-950"><header className="border-b bg-white"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-6"><Link href="/arbeitnehmer" className="text-2xl font-black">Stoyan<span className="text-blue-600">.</span></Link><nav className="hidden gap-7 text-sm font-semibold md:flex"><Link href="/arbeitnehmer" className="text-blue-600">Dashboard</Link><Link href="/arbeitnehmer/profil">Mein Profil</Link><Link href="#">Meine Matches</Link><Link href="#">Einstellungen</Link></nav><form action="/api/auth/signout" method="post"><button className="rounded-lg border px-4 py-2 text-sm font-bold">Abmelden</button></form></div></header><div className="mx-auto max-w-7xl px-6 py-10"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-bold uppercase tracking-widest text-blue-600">Arbeitnehmer</p><h1 className="mt-2 text-4xl font-black tracking-tight">Dein Profil. Deine Möglichkeiten.</h1><p className="mt-3 max-w-2xl text-slate-600">Präsentiere dich professionell und zeige Arbeitgebern, was du kannst.</p></div><Link href="/arbeitnehmer/profil" className="rounded-lg bg-blue-600 px-5 py-3 font-bold text-white">Profil bearbeiten</Link></div><div className="mt-10 grid gap-6 lg:grid-cols-[340px_1fr]"><section className="rounded-3xl border bg-white p-6 shadow-sm"><div className="flex items-center gap-4"><div className="h-24 w-24 overflow-hidden rounded-2xl bg-slate-100">{profile?.avatar_url ? <img src={profile.avatar_url} alt="Profilbild" className="h-full w-full object-cover"/> : <div className="flex h-full items-center justify-center text-3xl text-slate-400">+</div>}</div><div><p className="text-xl font-black">{profile?.first_name || "Vorname"} {profile?.last_name || "Nachname"}</p><p className="mt-1 text-sm text-slate-500">{employee?.profession || "Beruf noch nicht angegeben"}</p><p className="mt-1 text-sm text-slate-500">{profile?.city || "Ort noch nicht angegeben"}</p></div></div><div className="mt-7 rounded-2xl bg-slate-50 p-4"><div className="flex justify-between text-sm font-bold"><span>Profilstatus</span><span className="text-blue-600">{employee?.profile_visible ? "Sichtbar" : "Privat"}</span></div><p className="mt-2 text-sm leading-6 text-slate-500">Je vollständiger dein Profil, desto besser können passende Arbeitgeber dich einschätzen.</p></div></section><section className="grid gap-6 sm:grid-cols-2"><div className="rounded-3xl border bg-white p-6 shadow-sm"><p className="text-sm font-bold text-slate-400">PROFIL</p><h2 className="mt-2 text-2xl font-black">Berufliche Präsentation</h2><p className="mt-3 text-sm leading-6 text-slate-600">{employee?.headline || "Noch keine berufliche Überschrift hinterlegt."}</p><div className="mt-5 text-sm text-slate-500">Erfahrung: <b className="text-slate-900">{employee?.years_experience ?? 0} Jahre</b></div></div><div className="rounded-3xl border bg-white p-6 shadow-sm"><p className="text-sm font-bold text-slate-400">ARBEITSWUNSCH</p><h2 className="mt-2 text-2xl font-black">Was du suchst</h2><div className="mt-4 space-y-2 text-sm text-slate-600"><p>Pensum: <b className="text-slate-900">{employee?.desired_employment_percent ?? 100} %</b></p><p>Umkreis: <b className="text-slate-900">{employee?.preferred_radius_km ?? 30} km</b></p></div></div><div className="rounded-3xl border bg-slate-950 p-6 text-white shadow-sm sm:col-span-2"><p className="text-sm font-bold text-blue-300">NÄCHSTER SCHRITT</p><h2 className="mt-2 text-2xl font-black">Vervollständige dein Profil</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">Füge Foto, Ausbildung, Erfahrung, Skills und deine Wünsche hinzu. So entsteht dein vollständiges Stoyan-Profil.</p><Link href="/arbeitnehmer/profil" className="mt-6 inline-flex rounded-lg bg-white px-5 py-3 font-bold text-slate-950">Profil aufbauen →</Link></div></section></div></div></main>
}
