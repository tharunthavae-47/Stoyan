"use client"
import Link from "next/link"
import { useEffect,useState } from "react"
import { createClient } from "@/lib/supabase/client"
export default function BilderPage(){const [files,setFiles]=useState<File[]>([]);const [images,setImages]=useState<any[]>([]);const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const s=createClient();useEffect(()=>{(async()=>{const {data:{user}}=await s.auth.getUser();if(!user){location.href="/login";return}const {data}=await s.from("employee_images").select("id,image_url,image_name,image_position").eq("employee_id",user.id).order("image_position");setImages(data||[])})()},[]);async function upload(){setMessage("");if(!files.length)return;setBusy(true);const {data:{user}}=await s.auth.getUser();if(!user){setBusy(false);return}for(let i=0;i<files.length;i++){const file=files[i];if(!file.type.startsWith("image/")||file.size>10*1024*1024){setMessage("Bitte nur Bilder bis 10 MB verwenden.");continue}const ext=file.name.split(".").pop()?.toLowerCase()||"jpg";const path=`${user.id}/gallery-${Date.now()}-${i}.${ext}`;const up=await s.storage.from("employee-media").upload(path,file,{contentType:file.type});if(up.error){setMessage(up.error.message);continue}const {data:url}=s.storage.from("employee-media").getPublicUrl(path);await s.from("employee_images").insert({employee_id:user.id,image_url:url.publicUrl,image_name:file.name,image_position:images.length+i})}const {data}=await s.from("employee_images").select("id,image_url,image_name,image_position").eq("employee_id",user.id).order("image_position");setImages(data||[]);setFiles([]);setMessage("Bilder erfolgreich hochgeladen.");setBusy(false)}async function remove(id:string){await s.from("employee_images").delete().eq("id",id);setImages(x=>x.filter(i=>i.id!==id))}return <main className="min-h-screen bg-[#f7f8fa]"><header className="border-b bg-white"><div className="mx-auto flex h-20 max-w-5xl items-center justify-between px-6"><Link href="/arbeitnehmer" className="text-2xl font-black">Stoyan<span className="text-blue-600">.</span></Link><Link href="/arbeitnehmer/profil" className="text-sm font-bold">← Mein Profil</Link></div></header><div className="mx-auto max-w-5xl px-6 py-10"><p className="text-sm font-black uppercase tracking-widest text-blue-600">Meine Bilder</p><h1 className="mt-2 text-4xl font-black">Zeig mehr von dir.</h1><p className="mt-3 text-slate-600">Lade mehrere professionelle Bilder hoch, die dein Profil ergänzen.</p><section className="mt-8 rounded-3xl border bg-white p-7 shadow-sm"><input type="file" multiple accept="image/*" onChange={e=>setFiles(Array.from(e.target.files||[]))} className="block w-full rounded-xl border p-4"/><p className="mt-2 text-sm text-slate-500">JPG, PNG oder WEBP · max. 10 MB pro Bild</p><button onClick={upload} disabled={busy||!files.length} className="mt-5 rounded-xl bg-blue-600 px-6 py-3.5 font-bold text-white disabled:opacity-50">{busy?"Hochladen…":"Bilder hochladen"}</button>{message&&<p className="mt-4 rounded-xl bg-blue-50 p-3 text-sm font-semibold text-blue-800">{message}</p>}</section><section className="mt-8"><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{images.map(i=><div key={i.id} className="overflow-hidden rounded-2xl border bg-white shadow-sm"><img src={i.image_url} alt={i.image_name||"Profilbild"} className="aspect-square w-full object-cover"/><div className="flex items-center justify-between p-4"><span className="truncate text-sm font-semibold">{i.image_name||"Bild"}</span><button onClick={()=>remove(i.id)} className="text-sm font-bold text-red-600">Löschen</button></div></div>)}</div>{!images.length&&<div className="rounded-3xl border border-dashed bg-white p-10 text-center text-slate-500">Noch keine Bilder hochgeladen.</div>}</section></div></main>}
